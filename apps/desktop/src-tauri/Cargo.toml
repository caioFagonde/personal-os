[package]
name = "personal-os-desktop"
version = "0.4.0"
description = "Personal OS desktop shell"
edition = "2021"
license = "Proprietary"

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2.1", features = [] }

[features]
default = []
