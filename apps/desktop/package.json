{
  "name": "@personal-os/desktop",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-tauri-config.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml",
    "lint": "cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings",
    "format": "cargo fmt --manifest-path src-tauri/Cargo.toml -- --check"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "version": "0.7.0"
}
